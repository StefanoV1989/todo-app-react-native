import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    app: {
        backgroundColor: '#333333',
        height: '100%',
        padding: 20,
    },

    leftIcon: {
        paddingRight:10,
    },

    inputStyle: {
        color: '#FFFFFF'
    }
});