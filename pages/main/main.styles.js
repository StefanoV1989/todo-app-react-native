import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    app: {
        backgroundColor: '#333333',
        height: '100%',
    },

    aggiungi: {
        width: 120,
        position: 'absolute',
        bottom: 20,
        right: 20,
        
    }
});